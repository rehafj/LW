using UnityEngine;
using System.Collections;

public class SquidSpawnner : MonoBehaviour {
public GameObject []enemies;
public int num_enemtypes = 3;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	//or each enemy inlist clone it - instanciate it and pull it from list - move it -sleeeeep
	}
}
