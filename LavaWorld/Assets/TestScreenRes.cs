using UnityEngine;
using System.Collections;

public class TestScreenRes : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Screen.SetResolution(640, 480, true);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
